---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.1
  name: browsers.api.browserkube.io
spec:
  group: api.browserkube.io
  names:
    kind: Browser
    listKind: BrowserList
    plural: browsers
    singular: browser
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: Browser is the Schema for the browsers API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: BrowserSpec defines the desired state of Browser
            properties:
              browserName:
                type: string
              browserVersion:
                type: string
              caps:
                format: byte
                type: string
              enableVNC:
                description: 'nolint: tagliatelle'
                type: boolean
              enableVideo:
                description: video recording options
                type: boolean
              extensions:
                items:
                  properties:
                    extensionId:
                      type: string
                    updateUrl:
                      type: string
                    version:
                      type: string
                  type: object
                type: array
              platformName:
                type: string
              screenResolution:
                type: string
              timeZone:
                type: string
              type:
                type: string
            required:
            - browserName
            - browserVersion
            - type
            type: object
          status:
            description: BrowserStatus defines the observed state of Browser
            properties:
              host:
                type: string
              image:
                type: string
              message:
                type: string
              phase:
                description: |-
                  INSERT ADDITIONAL STATUS FIELD - define observed state of cluster
                  Important: Run "make" to regenerate code after modifying this file
                type: string
              podName:
                type: string
              portConfig:
                properties:
                  browser:
                    type: string
                  clipboard:
                    type: string
                  devTools:
                    type: string
                  fileServer:
                    type: string
                  sidecar:
                    type: string
                  vnc:
                    type: string
                type: object
              reason:
                type: string
              seleniumURL:
                type: string
              vncPass:
                type: string
            required:
            - phase
            - podName
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
