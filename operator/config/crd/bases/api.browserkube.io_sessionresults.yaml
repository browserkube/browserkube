---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.1
  name: sessionresults.api.browserkube.io
spec:
  group: api.browserkube.io
  names:
    kind: SessionResult
    listKind: SessionResultList
    plural: sessionresults
    singular: sessionresult
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: SessionResult is the Schema for the sessionresults API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SessionResultSpec defines the desired state of SessionResult
            properties:
              browser:
                description: BrowserSpec defines the desired state of Browser
                properties:
                  browserName:
                    type: string
                  browserVersion:
                    type: string
                  caps:
                    format: byte
                    type: string
                  enableVNC:
                    description: 'nolint: tagliatelle'
                    type: boolean
                  enableVideo:
                    description: video recording options
                    type: boolean
                  extensions:
                    items:
                      properties:
                        extensionId:
                          type: string
                        updateUrl:
                          type: string
                        version:
                          type: string
                      type: object
                    type: array
                  platformName:
                    type: string
                  screenResolution:
                    type: string
                  sessionIdleTimeout:
                    type: string
                  sessionTimeout:
                    description: timeouts
                    type: string
                  timeZone:
                    type: string
                  type:
                    type: string
                required:
                - browserName
                - browserVersion
                - type
                type: object
              browserImage:
                type: string
              files:
                properties:
                  bookmarks:
                    type: string
                  browserLog:
                    type: string
                  video:
                    type: string
                required:
                - bookmarks
                - browserLog
                - video
                type: object
              finishedAt:
                format: date-time
                type: string
              startedAt:
                format: date-time
                type: string
            required:
            - files
            type: object
          status:
            description: SessionResultStatus defines the observed state of SessionResult
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
