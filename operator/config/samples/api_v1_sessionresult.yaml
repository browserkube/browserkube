apiVersion: api.browserkube.io/v1
kind: SessionResult
metadata:
  labels:
    app.kubernetes.io/name: operator
    app.kubernetes.io/managed-by: kustomize
  name: sessionresult-sample
spec:
  # TODO(user): Add fields here
